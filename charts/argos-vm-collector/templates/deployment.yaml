apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "argos-vm-collector.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "argos-vm-collector.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "argos-vm-collector.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "argos-vm-collector.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "argos-vm-collector.serviceAccountName" . }}
      # The vm-collector never calls the Kubernetes API — it only talks to
      # the cloud provider and argosd over HTTPS. Skip projecting the SA
      # token so a compromised pod cannot pivot via the in-cluster token.
      automountServiceAccountToken: false
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: vm-collector
          image: {{ include "argos-vm-collector.image" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          {{- if .Values.metrics.enabled }}
          ports:
            - name: metrics
              containerPort: {{ .Values.metrics.port }}
              protocol: TCP
          {{- end }}
          env:
            - name: ARGOS_SERVER_URL
              value: {{ required "serverURL is required" .Values.serverURL | quote }}
            - name: ARGOS_VM_COLLECTOR_PROVIDER
              value: {{ .Values.account.provider | quote }}
            - name: ARGOS_VM_COLLECTOR_ACCOUNT_NAME
              value: {{ required "account.name is required" .Values.account.name | quote }}
            - name: ARGOS_VM_COLLECTOR_REGION
              value: {{ required "account.region is required" .Values.account.region | quote }}
            {{- if .Values.account.providerEndpoint }}
            - name: ARGOS_VM_COLLECTOR_PROVIDER_ENDPOINT_URL
              value: {{ .Values.account.providerEndpoint | quote }}
            {{- end }}
            - name: ARGOS_VM_COLLECTOR_INTERVAL
              value: {{ .Values.interval | quote }}
            - name: ARGOS_VM_COLLECTOR_FETCH_TIMEOUT
              value: {{ .Values.fetchTimeout | quote }}
            - name: ARGOS_VM_COLLECTOR_RECONCILE
              value: {{ .Values.reconcile | quote }}
            - name: ARGOS_VM_COLLECTOR_CREDENTIAL_REFRESH
              value: {{ .Values.credentialRefresh | quote }}
            {{- if .Values.metrics.enabled }}
            - name: ARGOS_VM_COLLECTOR_METRICS_ADDR
              value: "0.0.0.0:{{ .Values.metrics.port }}"
            {{- end }}
            {{- if .Values.argosTLS.caSecret }}
            - name: ARGOS_CA_CERT
              value: /etc/argos-vm-collector/argos-ca/{{ .Values.argosTLS.caKey }}
            {{- end }}
            {{- if .Values.argosTLS.existingSecret }}
            - name: ARGOS_CLIENT_CERT
              value: /etc/argos-vm-collector/argos-tls/tls.crt
            - name: ARGOS_CLIENT_KEY
              value: /etc/argos-vm-collector/argos-tls/tls.key
            {{- end }}
            {{- if .Values.argosTLS.extraHeaders }}
            - name: ARGOS_EXTRA_HEADERS
              value: {{ .Values.argosTLS.extraHeaders | quote }}
            {{- end }}
            {{- if .Values.proxy.httpsProxy }}
            - name: HTTPS_PROXY
              value: {{ .Values.proxy.httpsProxy | quote }}
            {{- end }}
            {{- if .Values.proxy.httpProxy }}
            - name: HTTP_PROXY
              value: {{ .Values.proxy.httpProxy | quote }}
            {{- end }}
            {{- if .Values.proxy.noProxy }}
            - name: NO_PROXY
              value: {{ .Values.proxy.noProxy | quote }}
            {{- end }}
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          envFrom:
            - secretRef:
                name: {{ required "tokenSecret.existingSecret is required" .Values.tokenSecret.existingSecret | quote }}
          volumeMounts:
            {{- if .Values.argosTLS.existingSecret }}
            - name: argos-tls
              mountPath: /etc/argos-vm-collector/argos-tls
              readOnly: true
            {{- end }}
            {{- if .Values.argosTLS.caSecret }}
            - name: argos-ca
              mountPath: /etc/argos-vm-collector/argos-ca
              readOnly: true
            {{- end }}
            {{- with .Values.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        {{- if .Values.argosTLS.existingSecret }}
        - name: argos-tls
          secret:
            secretName: {{ .Values.argosTLS.existingSecret | quote }}
        {{- end }}
        {{- if .Values.argosTLS.caSecret }}
        - name: argos-ca
          {{- if hasPrefix "configmap:" .Values.argosTLS.caSecret }}
          configMap:
            name: {{ trimPrefix "configmap:" .Values.argosTLS.caSecret | quote }}
            items:
              - key: {{ .Values.argosTLS.caKey }}
                path: {{ .Values.argosTLS.caKey }}
          {{- else }}
          secret:
            secretName: {{ .Values.argosTLS.caSecret | quote }}
            items:
              - key: {{ .Values.argosTLS.caKey }}
                path: {{ .Values.argosTLS.caKey }}
          {{- end }}
        {{- end }}
        {{- with .Values.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
