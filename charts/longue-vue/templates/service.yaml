apiVersion: v1
kind: Service
metadata:
  name: {{ include "longue-vue.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "longue-vue.labels" . | nindent 4 }}
    app.kubernetes.io/component: daemon
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "longue-vue.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: daemon
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
    {{- if and .Values.mcp.enabled (eq .Values.mcp.transport "sse") }}
    - name: mcp
      port: {{ .Values.service.mcpPort }}
      targetPort: mcp
      protocol: TCP
    {{- end }}
