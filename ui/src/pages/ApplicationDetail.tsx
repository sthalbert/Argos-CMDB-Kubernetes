// Application detail page placeholder. The full detail page (identity,
// curated metadata, DICT, member list, EOL summary) is implemented in
// ADR-0029 Phase 3 bundle B.

export function ApplicationDetail() {
  return (
    <div>
      <h2>Application</h2>
      <p className="muted">Application detail page (coming in Phase 3 bundle B).</p>
    </div>
  );
}

export default ApplicationDetail;
