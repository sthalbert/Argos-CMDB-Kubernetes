---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: argos
  namespace: argos-system
  labels:
    app.kubernetes.io/name: argos
    app.kubernetes.io/component: collector

---
# ClusterRole granting the bare minimum the collector needs to catalogue the
# cluster: list on the six resource kinds argosd currently ingests. No watch,
# no get-by-name, no write of any kind. Matches the cartography principle of
# least privilege — the CMDB is strictly a reader.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: argos-cluster-reader
  labels:
    app.kubernetes.io/name: argos
rules:
  - apiGroups: [""]
    resources: ["nodes", "namespaces", "pods", "services", "persistentvolumes", "persistentvolumeclaims"]
    verbs: ["list"]
  - apiGroups: ["apps"]
    resources: ["deployments", "statefulsets", "daemonsets", "replicasets"]
    verbs: ["list"]
  - apiGroups: ["networking.k8s.io"]
    resources: ["ingresses"]
    verbs: ["list"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: argos-cluster-reader
  labels:
    app.kubernetes.io/name: argos
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: argos-cluster-reader
subjects:
  - kind: ServiceAccount
    name: argos
    namespace: argos-system
