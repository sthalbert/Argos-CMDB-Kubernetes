apiVersion: apps/v1
kind: Deployment
metadata:
  name: longue-vue-vm-collector
  namespace: longue-vue-system
  labels:
    app.kubernetes.io/name: longue-vue
    app.kubernetes.io/component: vm-collector
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: longue-vue
      app.kubernetes.io/component: vm-collector
  template:
    metadata:
      labels:
        app.kubernetes.io/name: longue-vue
        app.kubernetes.io/component: vm-collector
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: longue-vue-vm-collector
          image: ghcr.io/sthalbert/longue-vue-vm-collector:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: longue-vue-vm-collector-config
            - secretRef:
                name: longue-vue-vm-collector-credentials
          ports:
            - name: metrics
              containerPort: 9090
              protocol: TCP
          resources:
            requests:
              cpu: 25m
              memory: 32Mi
            limits:
              cpu: 250m
              memory: 128Mi
          livenessProbe:
            tcpSocket:
              port: 9090
            initialDelaySeconds: 30
            periodSeconds: 30
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
